body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: #1890ff; color: white; padding: 20px; text-align: center; }
    .content { padding: 20px; background: #f5f5f5; }
    .task-details { background: white; padding: 15px; margin: 15px 0; border-left: 4px solid #1890ff; }
    .button { display: inline-block; padding: 10px 20px; background: #1890ff; color: white; text-decoration: none; border-radius: 4px; margin: 10px 0; }
    .footer { text-align: center; padding: 20px; color: #666; font-size: 12px; }
  


  
    
      🎯 New Task Assigned
    
    
      Hi {{assigneeName}},
      {{assignedByName}} has assigned you a new task:
      
      
        {{taskTitle}}
        Priority: {{priority}}
        Status: {{status}}
        {{#if dueDate}}
        Due Date: {{dueDate}}
        {{/if}}
        {{#if description}}
        Description:
        {{description}}
        {{/if}}
      
      
      View Task
    
    
      TaskFlow SaaS Platform
      You're receiving this email because you're a member of {{tenantName}}